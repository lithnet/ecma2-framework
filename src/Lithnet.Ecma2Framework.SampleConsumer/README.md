# Lithnet.Ecma2Framework.SampleConsumer

A connector that references the framework projects in this repository, not the packages. Building it triggers the host and shim builds through the in-repo targets, so it is the vehicle for in-repo end-to-end builds and for checking the worker output. Targets net8.0.
