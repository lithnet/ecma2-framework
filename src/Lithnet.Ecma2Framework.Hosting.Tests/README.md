# Lithnet.Ecma2Framework.Hosting.Tests

Unit tests for `Lithnet.Ecma2Framework.Hosting`: the orchestrators, the RPC target, the worker host, the packaged-MA manifest emitter, and the capability encoder. Targets net8.0.
