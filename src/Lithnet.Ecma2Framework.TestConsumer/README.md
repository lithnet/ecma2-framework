# Lithnet.Ecma2Framework.TestConsumer

A connector with stub providers used by the integration and hosting tests. Its providers return controlled data and record calls so the tests can assert behavior across the shim and worker. Targets net8.0.
