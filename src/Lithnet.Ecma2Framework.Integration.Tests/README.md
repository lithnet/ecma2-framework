# Lithnet.Ecma2Framework.Integration.Tests

End-to-end tests of the shim and worker over the named pipe. A real shim spawns a real worker and exercises schema, import, export, password, and capabilities through the full transport. Targets net48, the shim's framework.
